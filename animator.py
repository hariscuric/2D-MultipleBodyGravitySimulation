from vector import *

class animator:
    def __init__(self, positions : list[list[vector]]) -> None:
        self.positions = positions
        pass

    def animate(self, duration : float):
        return 0